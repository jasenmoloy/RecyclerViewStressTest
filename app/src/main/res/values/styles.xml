<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <style name="Card">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:orientation">vertical</item>
        <item name="android:background">@color/cardBackground</item>
    </style>

    <style name="CardContent">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="CardContent.Text">
        <item name="android:textColor">@android:color/darker_gray</item>
    </style>

    <style name="CardContent.LineBreak">
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_marginTop">2dp</item>
        <item name="android:layout_marginBottom">2dp</item>
        <item name="android:background">@android:color/darker_gray</item>
    </style>

    <style name="CardContent.Text.Title">
        <item name="android:textColor">@android:color/darker_gray</item>
        <item name="android:textSize">@dimen/card_title_textsize</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CardContent.ImgurImage">
        <item name="android:layout_height">300dp</item>
        <item name="android:paddingBottom">2dp</item>
    </style>

    <style name="CardContent.Text.Description">
        <item name="android:paddingBottom">2dp</item>
        <item name="android:textSize">@dimen/card_paragraph_textsize</item>
    </style>
</resources>
